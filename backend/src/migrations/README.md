This folder will contain TypeORM migration files.

Generate a migration with (from `backend/`):

```
npm run migration:generate -- -n MigrationName
```

Run pending migrations:

```
npm run migration:run
```
